# Liquibase properties for Maven plugin (mvn liquibase:update)
# Pass URL, username, password via -D or environment variables
changeLogFile=changelog/liquibase-changeLog.json
url=${DB_URL}
username=${USER_NAME}
password=${DB_PASSWORD}
driver=org.postgresql.Driver
